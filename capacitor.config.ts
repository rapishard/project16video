import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'project16video',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
